# Handoff - Phase 0 PR 0.1 (snapshot + smoke-disable)

**Sprint:** Scout canonical rewrite
**Plan:** `docs/plans/2026-04-22_canonical_rewrite_v5_1_merged.md` (§Phase 0 PR 0.1)
**Branch:** `sprint/phase-0-pr-0-1-snapshot-smoke-disable`
**Status:** ready for review
**Risk:** low (CI config edit + docs)

---

## What shipped in this PR

1. **CI edit:** `.github/workflows/ci.yml` `smoke-deployed.if:` narrowed from `github.event_name == 'push' || github.event_name == 'workflow_dispatch'` to `github.event_name == 'workflow_dispatch'`. Auto-trigger on push-to-main is off for the duration of Phase 1 through Phase 5. Workflow_dispatch remains. Original comment preserved in the re-enable checklist for exact reversal. TEMPORARY annotation added to the comment block so any reviewer during the sprint window sees context.

2. **Snapshot directory:** `docs/plans/_snapshots/README.md` documents the pg_dump command, filename convention, security note (repo is private), and the decision rationale for what the pre-sprint dump will contain.

3. **Re-enable checklist:** `docs/plans/2026-04-22_sprint_execution_reenable_checklist.md` is the authoritative reversal list for everything the sprint turns off - Railway env vars, the CI edit, Supabase Storage posture, manual-discipline items, and sprint-closure verification. Phase 5 PR 5.3 reverses these.

---

## What this PR does NOT do

- Does NOT produce `docs/plans/_snapshots/2026-04-22_pre_rewrite_full.sql`. That file is the output of `pg_dump` against Railway production; Claude Code does not have Railway credentials. Andrew runs the dump after this PR merges.
- Does NOT set any Railway env vars. That is PR 0.2's manual-ops scope (runs after PR 0.2 merges: `SCOUT_SCHEDULER_ENABLED=false`, `SCOUT_ENABLE_BOOTSTRAP=true`, `SCOUT_AI_ENABLED=false`).
- Does NOT add quiesce / unquiesce scripts. Those are PR 0.2's scope.
- Does NOT close any scout-ui clients. Andrew does that manually after PR 0.2's env flip.
- Does NOT touch any scout.* or public.* table schema. No migrations in this PR.

Snapshot file commits are deferred to a follow-up (either bundled with PR 0.2 or a separate direct-to-main docs commit after Andrew runs pg_dump). Either way, the snapshot must exist before Phase 1 PR 1.1 starts.

---

## Consumer audit

PR 0.1 is infrastructure-level and does not rewire any domain code, so the full §7 consumer-audit structure does not apply. For completeness:

### Source code (repo-wide grep)

**Searched for:** `smoke-deployed`, `push-to-main`, references to the CI trigger in docs.

**Scope:** `backend/`, `scout-ui/`, `smoke-tests/`, `scripts/`, `docs/`, `.github/`.

**Matches:**
- `.github/workflows/ci.yml` - the job itself. Edited.
- `docs/release_candidate_report.md` - references the smoke-deployed setup but does not hard-code the `if:` trigger. No edit needed.
- `docs/atr tasks/open_items.md` - historical references. No edit needed.
- Handoff docs under `docs/handoffs/` - historical references. No edit needed.
- `docs/plans/2026-04-22_canonical_rewrite_v5_preflight.md` - already documents the planned change. No edit needed.

No code consumers of the CI trigger beyond the workflow file itself.

### Database-side dependencies

N/A - no schema changes.

---

## Test plan

- [x] `node scripts/architecture-check.js` clean on branch (0 warnings, 1 persistent seedData INFO)
- [ ] CI on the PR: `backend-tests`, `frontend-types`, `arch-check` expected green; `smoke-deployed` expected to NOT run (that's the point of the edit; auto-trigger still active on this PR because this edit hasn't merged yet - the `push:` to main edit only affects post-merge pushes, not pre-merge PR events which use `pull_request:`)
- [ ] `smoke-web` remains optional per house rule

**Expected CI behavior:** all jobs run on this PR's `pull_request` event (as they do for any PR). On the merge commit, `smoke-deployed` will NOT fire (because the edit removed `push` from its trigger). This is the desired post-merge behavior.

---

## Andrew's manual actions after this PR merges

(Per v5.1 §Phase 0, deferred to after PR 0.2's script edits merge too.)

1. Set Railway env vars (`SCOUT_SCHEDULER_ENABLED=false`, `SCOUT_ENABLE_BOOTSTRAP=true`, `SCOUT_AI_ENABLED=false`).
2. Wait for deploy green.
3. Run `python scripts/quiesce_prod.py` (exists after PR 0.2).
4. Close all scout-ui clients.
5. Run `pg_dump` per `docs/plans/_snapshots/README.md` to produce `2026-04-22_pre_rewrite_full.sql`. Commit directly to main or as a follow-up PR.
6. Verbal / handoff-doc confirmation that manual ops completed before Phase 1 PR 1.1 starts.

---

## Stop-conditions honored

Per v5.1 §8 Phase 0 stop-conditions:

- Scheduler-ticking-after-flip: N/A for this PR (no env flip yet).
- Supabase Storage purge fail: N/A for this PR (no purge yet).
- Quiesce script fail: N/A for this PR (no script yet).
- Unplanned DB write detected: N/A for this PR (no DB touch).

None triggered.

---

## Open items flagged for Andrew

1. **Repo visibility confirmed PRIVATE** (`gh repo view` on 2026-04-23). The pre-sprint dump containing bcrypt hashes and live session tokens is acceptable to commit under that constraint. If repo visibility ever flips to public, those dump commits should be scrubbed or the history rewritten.

2. **`_snapshots/` directory is not gitignored.** The dump file will land inside git. This is intentional (forensic value) but worth explicit acknowledgment. Size expected small (low hundreds of rows across all tables).

3. **PR 0.1 does not hold the window open.** The CI edit takes effect the moment this PR merges, but the actual sprint window (env-flipped, clients-closed, snapshot-captured) doesn't open until after PR 0.2's manual ops. If Andrew pushes any unrelated commit to main between PR 0.1 merge and the window opening, no smoke-deployed auto-run happens (intended side effect: no noisy CI runs against intact prod either).

4. **PR 0.2 still ahead.** Per v5.1 §Phase 0 the next serial PR is 0.2: `scripts/quiesce_prod.py` + `scripts/unquiesce_prod.py` with `--ai-only` / `--full` flags. Do not start that until this PR merges and Andrew confirms green.

---

## Exit criteria (for Andrew's green-light)

- [ ] This PR merges cleanly on main.
- [ ] CI green on the branch (backend-tests, frontend-types, arch-check; smoke-deployed skipping is expected and fine).
- [ ] Andrew says "green, proceed to PR 0.2."

Do NOT start PR 0.2 until Andrew's explicit green.
