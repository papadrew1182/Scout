{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build:web"
  },
  "deploy": {
    "startCommand": "npx serve dist -s -l tcp://0.0.0.0:${PORT:-3000}",
    "healthcheckPath": "/"
  }
}
